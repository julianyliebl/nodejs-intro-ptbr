{
  "name": "nodejs-intro-ptbr",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest --coverage --forceExit --silent",
    "test:watch": "jest --watchAll",
    "test:ci": "jest --ci --runInBand --coverage"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageReporters": ["text", "lcov"],
    "testTimeout": 10000
  }
}
